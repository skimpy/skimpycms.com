{% extends 'layouts/master' %}

{% block metaDescription %}<meta name="description" content="{{ skimpy.site.meta_description }}">{% endblock %}

{% block title %}{{ skimpy.site.title }} - {{ skimpy.site.tagline }}{% endblock %}

{% block header %}
    <h1 id="site-title"><a href="{{ skimpy.url }}">{{ skimpy.site.title }}</a></h1>
    <p id="site-tagline">{{ skimpy.site.tagline }}</p>
{% endblock header %}

{% block content %}
    <div class="download section">
        <p>
            Skimpy is file based CMS that is written with and runs on PHP (Lumen). It is NOT a static site generator. Generators are cumbersome tools. Skimpy is easy to use. You just create a file, and there it is. Skimpy is so easy to use that it will likely be the easiest CMS/blogging tool that you've ever tried. Don't believe me? Give it a shot.
        </p>
        <div>
            <a href="{{ skimpy.url('docs') }}">Get Started</a>
        </div>
    </div> <!-- .hero -->
    <div class="features sections">
        <div class="feature">
            <div class="feature-header">
                <h4>Easy Content Creation</h4>
                <p>Want to create a new page or post?</p>
            </div>
            <ul>
                <li>Just create a new markdown file and your done</li>
                <li>All of your content and configuration is file based for easy version control</li>
            </ul>
        </div> <!-- .feature -->
        <div class="feature">
            <div class="feature-header">
                <h4>Categories & Tags</h4>
                <p>Building a blog?</p>
            </div>
            <ul>
                <li>Use YAML to define categories & tags</li>
                <li>Assign categories and tags with YAML front matter in your content files</li>
            </ul>
        </div> <!-- .feature -->
        <div class="feature">
            <div class="feature-header">
                <h4>Powerful Templating with Twig</h4>
                <p>Need custom templates for different page/post types?</p>
            </div>
            <ul>
                <li>Any content file under a directory will use the twig template with a name matching the directory name</li>
                <li>You can override templates in individual files using YAML front matter</li>
            </ul>
        </div> <!-- .feature -->
    </div>
    <div class="docs section">
        <h2><a class="link-no-style" href="{{ skimpy.url('docs') }}">Get Started</a></h2>
        <a href="{{ skimpy.url('docs') }}">Read the Docs</i></a>
    </div> <!-- .docs -->
{% endblock content %}

{% block footer %}
    <h4>Monthly Newsletter</h4>
    <p>Get updates on new Skimpy features. We have a lot of cool stuff planned!</p>
    <form>
        <input type="email" name="email" placeholder="you@example.com" />
        <button>Subscribe</button>
    </form>
{% endblock %}